{% extends 'base.html.twig' %}

{% block title %}Edit Morceaux{% endblock %}

{% block body %}
    <h1>Edit Morceaux</h1>

    {{ include('morceaux/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_morceaux_index') }}">back to list</a>

    {{ include('morceaux/_delete_form.html.twig') }}
{% endblock %}
