<form method="post" action="{{ path('app_morceaux_delete', {'id': morceaux.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ morceaux.id) }}">
    <button class="btn">Delete</button>
</form>
